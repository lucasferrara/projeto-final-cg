function addTicket(x, z) {
    const paperColor = [1.0, 1.0, 1.0]; 
    const textColor = [0.2, 0.2, 0.2];  
    const y = 1.0; 
    addCube(x, y, z, 0.25, 0.15, 0.02, paperColor);
    addCube(x, y, z + 0.03, 0.15, 0.02, 0.005, textColor);
}
